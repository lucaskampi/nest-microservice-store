# nest-microservice-store

# nest-microservice-store

Minimal NestJS backend starter (TypeScript).

Run the backend:

```bash
cd backend
npm install
npm run dev
```

Backend runs on http://localhost:4000/api

This is intentionally minimal for a "first commit".
# nest-microservice-store
